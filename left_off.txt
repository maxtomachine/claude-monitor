claude-monitor — left off 2026-05-11 (session: tools-monitor / 7a5651f1)

SHIPPED TO MAIN (PRs #6-#11, all merged):
- #6  Recycled-PID ghost sessions (mdworker case) — _pid_is_claude check
- #7  ◐ BUSY status when subagents/workflows running (rglob into nested dirs)
- #8  Double-click row → jump; single-click highlights only (SessionTable subclass)
- #9  Delete-Delete hide in history mode + Shift+arrow/click multi-select
      + cursor-survivor on hide + view-state persistence across Shift+R
- #10 Hook reads transcript custom-title (canonical) so window/row/state never desync
      + duplicate row titles get ·sid8 suffix
- #11 Booting-session ghost suppressed (skip <5s no-name in orphan-PID pass)
      + transcript-mtime → WORKING override (slash commands miss UserPromptSubmit)
      + install.sh symlinks hook (was cp → 5wk stale)
      + jumpback launches Ghostty window if no monitor found
      + launcher calls .venv/bin/python directly (offline + no-PATH safe)
      + session column elastic (absorbs slack, shrinks last)
      + ctx-% estimate uses MODEL_CONTEXT_WINDOW + ~N% marker

ALSO:
- /catchmeup script fixed (PID-ancestry detection — was misidentifying current session)
- ~/.claude/hooks/session_tracker.py relinked to repo (was stale copy from Mar 29)

KNOWN FOLLOW-UPS (not blocking):
- ctx-% deeper fix: read remaining_percentage from transcript usage block instead of
  token-count estimate (the estimate is now per-model-window but still ignores
  compaction/snips; ~prefix marks it)
- _refresh_pid_map:958 still uses raw os.kill not _pid_is_claude (perf concern,
  not currently causing ghosts)
- The 15 minimal {pid,tty,sid} state files from Apr 9 are dead-PID cruft, harmless

CLICK-TO-JUMP (2026-05-21, all on main):
- claude-jump CLI + /tmp/claude-jump-request file + monitor 200ms poller
- http://localhost:48624/jump/<sid8> listener in monitor (Chrome flashes, then
  the raised window covers it) — THE clickable path; shortcuts:// is not
  clickable in terminal output, Shortcuts sandbox can't send Apple Events
- ClaudeJump macOS Shortcut exists (manual GUI creation, Santa blocks the rest)
  but is no longer in the critical path — useful for Spotlight/hotkey only
- Global ~/.claude/CLAUDE.md convention: mention a live session → append its
  jump URL. Verified cross-window.
- FOLLOW-UP: migrate all /tmp/claude-* signal files to mode-700 ~/.claude/run/
  in one pass (security review flags the fixed-path-in-/tmp pattern)
- FOLLOW-UP: Ghostty notification clicks can't focus the originating window
  across Spaces — unimplemented upstream (ghostty#9145), not fixable here

POST-#11 DIRECT TO MAIN (2026-05-11/12):
- 4c052aa  Statusline cache persists to ~/.claude/statusline-cache/ (survives reboot)
- 7854434  Hook 'exited' ignored when alive=True (subagent SessionEnd pollutes parent)
- bd6e48b  Delete works outside history mode on closed/archived; unknown ctx → '?' not ~N%

main is at bd6e48b. 224 tests passing. All local feature branches pruned.

OPEN HOOK-SIDE FOLLOW-UP: session_tracker.py mark_exited() should guard against
subagent SessionEnd writing to parent state (parallel to the pre_tool_use guard
at :513-519). Monitor-side guard at determine_status:1239 catches it now, but
the state file itself is still wrong until the parent's next hook event.

DEAD SYMLINK: ~/.claude/statusline.sh -> ~/claude-monitor/... (no Projects/).
Harmless — settings.json points directly at the repo path. Re-run install.sh
or `ln -sf ~/Projects/claude-monitor/statusline/statusline.sh ~/.claude/statusline.sh`
to clean it up.
